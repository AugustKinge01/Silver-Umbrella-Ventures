{
  "name": "silver-umbrella-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Silver Umbrella DePIN network on OneChain",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network onechainTestnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network onechainMainnet",
    "deploy:localhost": "hardhat run scripts/deploy.js --network localhost",
    "verify": "hardhat verify --network onechainTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0",
    "dotenv": "^16.3.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0"
  }
}
